services:
  app:
    container_name: api-usuarios
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - SQLALCHEMY_DATABASE_URL="postgresql+psycopg2://usuario:contraseña@localhost:5432/nombre_base_datos"
    networks:
      - internal_network
      
networks:
  internal_network:
    external: true